{
    "name": "ayush-ai-monorepo",
    "version": "1.0.0",
    "private": true,
    "description": "Monorepo for ayush.ai frontend and backend",
    "workspaces": ["frontend", "backend"],
    "scripts": {
        "check": "npm run check --workspace frontend && npm run check --workspace backend",
        "build": "npm run build --workspace frontend && npm run build --workspace backend",
        "dev:frontend": "serve frontend -l 5500",
        "dev:backend": "node --watch backend/src/server.js"
    },
    "engines": {
        "node": ">=20"
    }
}
